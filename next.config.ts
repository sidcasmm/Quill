import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Full SSR on Vercel. Do not set `output: "export"` — that would disable
  // server-rendered post pages, metadata, and sitemap generation.
};

export default nextConfig;
